#All built in fucntion

#a

abs() #absolute value retruns when we give a floating or complex number
all() #it takes a iterable data type inside and it give a bool data type, if the
      #iterable given is empty it takes as true *if the all element of iterable
      #is True means return True else false as a AND operator
any() #it takes a iterable data type inside and it give a bool data type, if the
      #iterable given is empty it takes as false *if the any element of iterable
      #is True means return True else false as a OR operator
ascii()#makes a object to printable like one
